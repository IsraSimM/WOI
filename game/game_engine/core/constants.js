import { CELL as MAP_CELL } from '../generation/world_generation/map.js';

export const CELL = MAP_CELL;

export const DEFAULTS = {
  cellSize: 6,
  wallHeight: 2,
  playerHeight: 1.6,
  playerRadius: 0.35,
  enemySpeed: 1.4,
};
